[project]
name = "dsw"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Anmol Deep", email = "adsk2050@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "google-genai>=2.22.0",
    "httpx>=0.28.1",
    "pydantic>=2.13.5",
    "python-dotenv>=1.2.3",
    "rich>=15.0.0",
    "tenacity>=9.1.4",
]

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
